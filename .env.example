# Copy to .env (gitignored) and fill in. Never commit .env.

# TypeSafe Jev — bill scoring. https://typesafe.ai
TYPESAFE_API_KEY=

# OpenRouter — the superseded LLM rubric, kept for the Stage 1 comparison.
OPENROUTER_API_KEY=
